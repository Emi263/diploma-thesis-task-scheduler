import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  view: {},
  title: {
    fontSize: 30,
    textAlign: "center",
  },

  singleTask: {
    backgroundColor: "red",
    display: "flex",
  },
});
